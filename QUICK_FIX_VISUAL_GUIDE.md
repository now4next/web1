# 🚀 빠른 해결 가이드 (스크린샷 포함)

## 📊 현재 상황
```
✅ 계정 인증: 성공 (nowfornext@gmail.com)
✅ 계정 권한: Super Administrator - All Privileges
❌ D1 접근: 실패 (Authentication error [code: 10000])
```

**문제**: API 토큰에 D1 권한이 없음 (계정 권한과 API 토큰 권한은 별개)

---

## 🎯 즉시 해결 방법 (2가지)

---

## ✅ 방법 1: Cloudflare Console에서 직접 작업 (추천 - 5분)

### 왜 이 방법을 추천하나요?
- ✅ **즉시 실행 가능** - API 토큰 수정 불필요
- ✅ **100% 작동 보장** - 웹 콘솔은 항상 접근 가능
- ✅ **시각적 확인** - 실시간으로 결과 확인

### 📝 단계별 가이드

#### Step 1: Cloudflare Dashboard 접속
```
🌐 URL: https://dash.cloudflare.com
📧 로그인: nowfornext@gmail.com
```

#### Step 2: D1 데이터베이스로 이동
```
클릭 순서:
1. 좌측 메뉴 "Workers & Pages" 클릭
2. 상단 탭에서 "D1" 클릭
3. "aiassess-production" 클릭
4. "Console" 탭 클릭
```

**화면 구성**:
```
┌─────────────────────────────────────────────────────────┐
│ Cloudflare Dashboard                                     │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Workers & Pages   D1   R2   Queues                     │
│  ─────────────────────────────────────────              │
│                                                          │
│  📊 D1 Databases                                         │
│                                                          │
│  ┌──────────────────────────────────────────┐           │
│  │ aiassess-production                      │ ← 클릭    │
│  │ Database ID: c8ebe20f-...                │           │
│  │ Created: 2024-XX-XX                      │           │
│  └──────────────────────────────────────────┘           │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

#### Step 3: SQL 실행 (Console 화면)
```
┌─────────────────────────────────────────────────────────┐
│ aiassess-production                                      │
├─────────────────────────────────────────────────────────┤
│  Tables   Console   Metrics   Settings                  │
│           ───────                                        │
│                                                          │
│  ┌────────────────────────────────────────────┐         │
│  │ -- Enter your SQL query here              │         │
│  │ SELECT * FROM competencies LIMIT 10;      │         │
│  │                                            │         │
│  │                                            │         │
│  └────────────────────────────────────────────┘         │
│                                                          │
│              [Execute Query] 버튼 ← 클릭                │
│                                                          │
│  Results:                                                │
│  ┌────────────────────────────────────────────┐         │
│  │ id | keyword | description | ...          │         │
│  │ ─────────────────────────────────────────  │         │
│  │ 1  | 리더십   | ...                       │         │
│  └────────────────────────────────────────────┘         │
└─────────────────────────────────────────────────────────┘
```

#### Step 4: 복사-붙여넣기로 SQL 실행

**A. job_name 업데이트 (30개 UPDATE)**

Console 창에 다음을 **복사-붙여넣기**:

```sql
-- 영업 직무 (3개)
UPDATE competencies SET job_name = '영업' WHERE keyword = '고객관계 구축';
UPDATE competencies SET job_name = '영업' WHERE keyword = '설득/협상';
UPDATE competencies SET job_name = '영업' WHERE keyword = '시장분석';

-- 마케팅 직무 (3개)
UPDATE competencies SET job_name = '마케팅' WHERE keyword = '시장분석';
UPDATE competencies SET job_name = '마케팅' WHERE keyword = '창의적 사고';
UPDATE competencies SET job_name = '마케팅' WHERE keyword = '커뮤니케이션';

-- 인사 직무 (3개)
UPDATE competencies SET job_name = '인사' WHERE keyword = '커뮤니케이션';
UPDATE competencies SET job_name = '인사' WHERE keyword = '공정성/윤리';
UPDATE competencies SET job_name = '인사' WHERE keyword = '전략적 사고/기획';

-- 재무 직무 (3개)
UPDATE competencies SET job_name = '재무' WHERE keyword = '분석적 사고';
UPDATE competencies SET job_name = '재무' WHERE keyword = '정확성/세심함';
UPDATE competencies SET job_name = '재무' WHERE keyword = '의사결정/판단력';

-- IT 직무 (3개)
UPDATE competencies SET job_name = 'IT' WHERE keyword = '문제해결';
UPDATE competencies SET job_name = 'IT' WHERE keyword = '혁신/개선';
UPDATE competencies SET job_name = 'IT' WHERE keyword = '기술적 전문성';

-- 생산관리 직무 (3개)
UPDATE competencies SET job_name = '생산관리' WHERE keyword = '프로세스 개선';
UPDATE competencies SET job_name = '생산관리' WHERE keyword = '품질관리';
UPDATE competencies SET job_name = '생산관리' WHERE keyword = '계획/조직화';

-- 구매조달 직무 (3개)
UPDATE competencies SET job_name = '구매조달' WHERE keyword = '협상/설득';
UPDATE competencies SET job_name = '구매조달' WHERE keyword = '분석적 사고';
UPDATE competencies SET job_name = '구매조달' WHERE keyword = '관계구축';

-- 물류 직무 (3개)
UPDATE competencies SET job_name = '물류' WHERE keyword = '계획/조직화';
UPDATE competencies SET job_name = '물류' WHERE keyword = '효율성 추구';
UPDATE competencies SET job_name = '물류' WHERE keyword = '문제해결';

-- 품질관리 직무 (3개)
UPDATE competencies SET job_name = '품질관리' WHERE keyword = '정확성/세심함';
UPDATE competencies SET job_name = '품질관리' WHERE keyword = '분석적 사고';
UPDATE competencies SET job_name = '품질관리' WHERE keyword = '개선지향';

-- 연구개발 직무 (3개)
UPDATE competencies SET job_name = '연구개발' WHERE keyword = '창의적 사고';
UPDATE competencies SET job_name = '연구개발' WHERE keyword = '혁신/개선';
UPDATE competencies SET job_name = '연구개발' WHERE keyword = '기술적 전문성';
```

👆 **[Execute Query]** 버튼 클릭

**예상 결과**: `30 rows affected` 또는 유사한 메시지

---

**B. 중복 역량 제거 (8개 DELETE)**

새 쿼리 창에 다음을 **복사-붙여넣기**:

```sql
-- Step 1: 외래키 제약 비활성화
PRAGMA foreign_keys = OFF;

-- Step 2: 행동지표 먼저 삭제
DELETE FROM behavioral_indicators 
WHERE competency_id IN (
  SELECT c.id 
  FROM competencies c
  JOIN competency_models cm ON c.model_id = cm.id
  WHERE cm.name = '경영지원 직무역량'
  AND c.keyword IN (
    '분석적 사고', 
    '의사결정/판단력', 
    '전략적 사고/기획', 
    '창의적 사고', 
    '리더십', 
    '문제해결', 
    '시장분석', 
    '커뮤니케이션'
  )
);

-- Step 3: 중복 역량 삭제
DELETE FROM competencies 
WHERE id IN (
  SELECT c.id 
  FROM competencies c
  JOIN competency_models cm ON c.model_id = cm.id
  WHERE cm.name = '경영지원 직무역량'
  AND c.keyword IN (
    '분석적 사고', 
    '의사결정/판단력', 
    '전략적 사고/기획', 
    '창의적 사고', 
    '리더십', 
    '문제해결', 
    '시장분석', 
    '커뮤니케이션'
  )
);

-- Step 4: 외래키 제약 재활성화
PRAGMA foreign_keys = ON;
```

👆 **[Execute Query]** 버튼 클릭

**예상 결과**: 
- 행동지표 삭제: `XX rows affected`
- 역량 삭제: `8 rows affected`

---

**C. 결과 확인**

```sql
-- 1. 전체 역량 개수 (45개 예상)
SELECT COUNT(*) as total FROM competencies;

-- 2. job_name별 역량 개수
SELECT job_name, COUNT(*) as count 
FROM competencies 
WHERE job_name IS NOT NULL
GROUP BY job_name
ORDER BY job_name;

-- 3. 중복 확인 (0개 예상)
SELECT keyword, COUNT(*) as count 
FROM competencies 
GROUP BY keyword 
HAVING COUNT(*) > 1;

-- 4. 모델별 역량 개수
SELECT cm.name, COUNT(c.id) as count
FROM competency_models cm
LEFT JOIN competencies c ON cm.id = c.model_id
GROUP BY cm.name;
```

**예상 결과**:
```
total: 45

job_name별:
IT: 3개
구매조달: 3개
마케팅: 3개
물류: 3개
생산관리: 3개
영업: 3개
인사: 3개
재무: 3개
연구개발: 3개
품질관리: 3개

중복: 0개

모델별:
경영지원 직무역량: 15개
역량 평가표: 30개
```

---

## ✅ 방법 2: API 토큰 권한 추가 (영구 해결 - 10분)

### 이 방법을 선택하는 경우
- ✅ CLI로 작업하고 싶은 경우
- ✅ 자동화/스크립트가 필요한 경우
- ✅ 여러 번 D1 작업을 할 예정인 경우

### 📝 단계별 가이드

#### Step 1: API Tokens 페이지 접속
```
🌐 URL: https://dash.cloudflare.com/profile/api-tokens
또는
Dashboard → 우측 상단 프로필 → My Profile → API Tokens
```

#### Step 2: 토큰 찾기 또는 생성

**화면**:
```
┌─────────────────────────────────────────────────────────┐
│ API Tokens                                               │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  [Create Token]  버튼                                    │
│                                                          │
│  Your API Tokens:                                        │
│  ┌──────────────────────────────────────────┐           │
│  │ GenSpark Token                           │           │
│  │ Created: 2024-XX-XX                      │           │
│  │ Permissions: Workers Scripts:Edit, ...   │           │
│  │                                          │           │
│  │         [Roll] [Edit] [Delete]           │           │
│  └──────────────────────────────────────────┘           │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

**선택**:
- 기존 토큰 **[Edit]** 클릭 (권장)
- 또는 **[Create Token]** 클릭 (새로 생성)

#### Step 3: 권한 추가

**편집 화면**:
```
┌─────────────────────────────────────────────────────────┐
│ Edit Token                                               │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Permissions:                                            │
│                                                          │
│  ┌────────────┬──────────────────┬──────────┐           │
│  │ Type       │ Permission       │ Access   │           │
│  ├────────────┼──────────────────┼──────────┤           │
│  │ Account    │ Workers Scripts  │ Edit     │ ✅        │
│  │ Account    │ D1               │ Edit     │ ← 추가!   │
│  │ Zone       │ Workers Routes   │ Edit     │ ✅        │
│  └────────────┴──────────────────┴──────────┘           │
│                                                          │
│  [+ Add more] ← 클릭해서 D1:Edit 추가                    │
│                                                          │
│  Account Resources:                                      │
│  ☑ Include  Nowfornext@gmail.com's Account              │
│                                                          │
│  Zone Resources:                                         │
│  ☑ Include  All zones                                   │
│                                                          │
│             [Continue to summary]                        │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

**추가할 권한**:
1. **[+ Add more]** 클릭
2. Type: **Account** 선택
3. Permission: **D1** 선택
4. Access: **Edit** 선택
5. **[Continue to summary]** 클릭
6. **[Update Token]** 클릭

#### Step 4: GenSpark에 토큰 등록

**새 토큰 생성한 경우만**:
1. 토큰 복사 (⚠️ 한 번만 표시됨!)
2. GenSpark 인터페이스 → **Deploy** 탭
3. Cloudflare API Key → **[Update]**
4. 토큰 붙여넣기 → **[Save]**

#### Step 5: 검증

다시 샌드박스에서:
```bash
# 1. D1 목록 확인 (이제 작동해야 함)
cd /home/user/webapp
npx wrangler d1 list

# 2. 원격 쿼리 테스트
npx wrangler d1 execute aiassess-production --remote --command="SELECT COUNT(*) FROM competencies"

# 3. job_name 업데이트
npx wrangler d1 execute aiassess-production --remote --file=./update_job_names.sql

# 4. 중복 제거
npx wrangler d1 execute aiassess-production --remote --file=./remove_duplicates.sql
```

---

## 🎯 추천 흐름

### 지금 당장 (5분)
➡️ **방법 1 선택**: Cloudflare Console에서 직접 실행
- job_name 업데이트 완료 ✅
- 중복 제거 완료 ✅
- 결과 확인 ✅

### 나중에 (선택 사항)
➡️ **방법 2 수행**: API 토큰 권한 추가
- 향후 CLI 작업을 위해
- 자동화를 위해

---

## ✅ 최종 검증

모든 작업 완료 후:

```bash
# 1. 직무명 검색 테스트
curl "https://aiassess.pages.dev/api/competencies/search?q=영업"
# 예상: 3개 역량 반환

# 2. 키워드 검색 테스트
curl "https://aiassess.pages.dev/api/competencies/search?q=리더십"
# 예상: 1개 역량 반환 (중복 제거됨)

# 3. 전체 개수 확인
curl "https://aiassess.pages.dev/api/competencies/search" | jq '.data | length'
# 예상: 45
```

---

## 📞 추가 도움말

### Console에서 SQL이 실행되지 않는 경우
- 여러 줄 SQL은 한 번에 실행되지 않을 수 있음
- 각 UPDATE/DELETE를 개별적으로 실행
- 또는 세미콜론(;)으로 구분된 문장을 순차 실행

### API 토큰 권한이 계속 안 되는 경우
- 토큰 재생성 권장
- "Custom token" 템플릿 사용
- D1:Edit 권한 명시적으로 추가
- Account Resources에 계정 포함 확인

### 더 많은 정보
- `API_TOKEN_FIX_GUIDE.md` - 상세 가이드
- `PRODUCTION_MANUAL_UPDATE.md` - SQL 전체 코드
- `DEPLOYMENT_STATUS.md` - 현재 상태 요약

**질문이 있으시면 언제든지 알려주세요! 🚀**
